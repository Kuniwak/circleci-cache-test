dependencies:
  cache_directories:
    - "~/hoge"
  pre:
    - "mkdir -p ~/hoge"
    - "ls -la ~/hoge"
  override:
    - "# noop"

test:
  override:
    - "date >> ~/hoge/file"
    - "cat ~/hoge/file"
