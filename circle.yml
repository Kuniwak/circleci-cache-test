dependencies:
  cache_directories:
    - "~/hoge"
  pre:
    - "mkdir -p ~/hoge"
    - "ls -la ~/hoge"
  override:
    - "date >> ~/hoge/file"
    - "./node.sh"

test:
  override:
    - "cat ~/hoge/file"
    - "./test.sh"
